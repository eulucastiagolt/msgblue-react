import React from 'react';

export default function Chat()
{
    const contentStyle = {
        padding: "10px 10px",
    };

    return(
        <div style={contentStyle}>
            <div>

            </div>
        </div>
    );
}